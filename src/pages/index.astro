---
import LanguageSwitcher from '../components/LanguageSwitcher.astro';

const currentLang = 'es';
---

<!DOCTYPE html>
<html lang={currentLang}>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Proyecto Opcean Discord Bot - Términos y Condiciones</title>
  <meta name="description" content="Términos y Condiciones para el ecosistema de bots Discord Proyecto Opcean Discord Bot">
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
</head>
<body>
  <div class="landing-container">
    <div class="background-overlay"></div>
    
    <header class="landing-header">
      <LanguageSwitcher currentLang={currentLang} />
    </header>
    
    <main class="landing-main">
      <div class="intro-section">
        <h1 class="main-title">
          <span class="project-name">Proyecto Opcean Discord Bot</span>
          <span class="subtitle">Términos y Condiciones</span>
        </h1>
        
        <p class="intro-text">
          Bienvenido al ecosistema de bots Discord con personalidades kawaii únicas. 
          Selecciona el bot para ver sus términos y condiciones específicos:
        </p>
      </div>
      
      <div class="bots-selection">
        <a href="/sakura/" class="bot-button sakura-bot">
          <div class="bot-icon"><img src="/images/bots/sakura.jpg" alt="Sakura" style="width: 60px; height: 60px; border-radius: 50%; object-fit: cover;"></div>
          <div class="bot-info">
            <h3>Sakura</h3>
            <p>Especialista en IA Avanzada</p>
          </div>
          <div class="glow-effect"></div>
        </a>
        
        <a href="/nebula/" class="bot-button nebula-bot">
          <div class="bot-icon"><img src="/images/bots/nebula.jpg" alt="Nebula" style="width: 60px; height: 60px; border-radius: 50%; object-fit: cover;"></div>
          <div class="bot-info">
            <h3>Nebula</h3>
            <p>Bot Multi-Función Principal</p>
          </div>
          <div class="glow-effect"></div>
        </a>
        
        <a href="/archan/" class="bot-button archan-bot">
          <div class="bot-icon"><img src="/images/bots/archan.jpg" alt="Archan" style="width: 60px; height: 60px; border-radius: 50%; object-fit: cover;"></div>
          <div class="bot-info">
            <h3>Archan</h3>
            <p>Asistente de Programación Kawaii</p>
          </div>
          <div class="glow-effect"></div>
        </a>
      </div>
    </main>
    
    <footer class="landing-footer">
      <p>Desarrollado por OpceanAI</p>
    </footer>
  </div>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #000000 0%, #1a0033 50%, #2d1b69 100%);
      color: white;
      min-height: 100vh;
      overflow-x: hidden;
    }

    .landing-container {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      position: relative;
    }

    .background-overlay {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: 
        radial-gradient(circle at 20% 80%, rgba(255, 182, 193, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 20%, rgba(221, 160, 221, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 40% 40%, rgba(100, 149, 237, 0.1) 0%, transparent 50%);
      z-index: -1;
      pointer-events: none;
    }

    .landing-header {
      position: absolute;
      top: 2rem;
      right: 2rem;
      z-index: 10;
    }

    .landing-main {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 2rem;
      text-align: center;
    }

    .intro-section {
      margin-bottom: 4rem;
      max-width: 800px;
    }

    .main-title {
      margin-bottom: 2rem;
    }

    .project-name {
      display: block;
      font-size: clamp(3rem, 6vw, 5rem);
      font-weight: 800;
      background: linear-gradient(135deg, #FFB6C1, #DDA0DD, #6495ED);
      background-clip: text;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 1rem;
      animation: glow 3s ease-in-out infinite alternate;
    }

    .subtitle {
      display: block;
      font-size: clamp(1.5rem, 3vw, 2rem);
      font-weight: 300;
      color: rgba(255, 255, 255, 0.8);
      font-style: italic;
    }

    .intro-text {
      font-size: 1.2rem;
      line-height: 1.6;
      color: rgba(255, 255, 255, 0.9);
      max-width: 600px;
      margin: 0 auto;
    }

    .bots-selection {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      max-width: 1200px;
      width: 100%;
    }

    .bot-button {
      position: relative;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 2.5rem 2rem;
      background: rgba(255, 255, 255, 0.05);
      border: 2px solid rgba(255, 255, 255, 0.1);
      border-radius: 20px;
      text-decoration: none;
      color: white;
      transition: all 0.3s ease;
      backdrop-filter: blur(10px);
      overflow: hidden;
      cursor: pointer;
    }

    .bot-button:hover {
      transform: translateY(-10px);
      border-color: rgba(255, 255, 255, 0.3);
    }

    .bot-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
      z-index: 2;
    }

    .bot-info h3 {
      font-size: 1.4rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
      z-index: 2;
    }

    .bot-info p {
      font-size: 1rem;
      color: rgba(255, 255, 255, 0.8);
      z-index: 2;
    }

    .glow-effect {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      border-radius: 20px;
      opacity: 0;
      transition: opacity 0.3s ease;
      z-index: 1;
    }

    /* Sakura Bot - Rosa */
    .sakura-bot:hover .glow-effect {
      opacity: 1;
      background: linear-gradient(135deg, rgba(255, 182, 193, 0.3), rgba(255, 105, 180, 0.2));
      box-shadow: 0 0 30px rgba(255, 182, 193, 0.4), inset 0 0 30px rgba(255, 182, 193, 0.1);
    }

    .sakura-bot:hover {
      border-color: #FFB6C1;
      box-shadow: 0 10px 40px rgba(255, 182, 193, 0.3);
    }

    /* Nebula Bot - Morado */
    .nebula-bot:hover .glow-effect {
      opacity: 1;
      background: linear-gradient(135deg, rgba(221, 160, 221, 0.3), rgba(147, 112, 219, 0.2));
      box-shadow: 0 0 30px rgba(221, 160, 221, 0.4), inset 0 0 30px rgba(221, 160, 221, 0.1);
    }

    .nebula-bot:hover {
      border-color: #DDA0DD;
      box-shadow: 0 10px 40px rgba(221, 160, 221, 0.3);
    }

    /* Archan Bot - Azul marino */
    .archan-bot:hover .glow-effect {
      opacity: 1;
      background: linear-gradient(135deg, rgba(100, 149, 237, 0.3), rgba(30, 144, 255, 0.2));
      box-shadow: 0 0 30px rgba(100, 149, 237, 0.4), inset 0 0 30px rgba(100, 149, 237, 0.1);
    }

    .archan-bot:hover {
      border-color: #6495ED;
      box-shadow: 0 10px 40px rgba(100, 149, 237, 0.3);
    }

    .landing-footer {
      text-align: center;
      padding: 2rem;
      color: rgba(255, 255, 255, 0.6);
      font-size: 0.9rem;
    }

    @keyframes glow {
      from { filter: drop-shadow(0 0 20px rgba(255, 182, 193, 0.3)); }
      to { filter: drop-shadow(0 0 30px rgba(100, 149, 237, 0.5)); }
    }

    @media (max-width: 768px) {
      .landing-header {
        position: relative;
        top: 0;
        right: 0;
        text-align: center;
        padding: 1rem;
      }

      .landing-main {
        padding: 1rem;
      }

      .intro-section {
        margin-bottom: 3rem;
      }

      .bots-selection {
        grid-template-columns: 1fr;
        gap: 1.5rem;
      }

      .bot-button {
        padding: 2rem 1.5rem;
      }
    }
  </style>
</body>
</html>
